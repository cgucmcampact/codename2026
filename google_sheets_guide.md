# 🧾 百草醫館 - Google Sheets 資料庫欄位編輯說明書

本說明書旨在協助醫館大掌櫃理解與編輯 Google 試算表（Google Sheets）中各個工作表的資料結構，以便手動調整遊戲參數或弟子資料。

---

## 📁 1. system_config (系統設定工作表)
此表用於儲存全域系統狀態與經驗值 (XP) 參數，結構為簡單的 `鍵-值 (Key-Value)` 對。

| 鍵 (key) | 預設值 (value) | 說明與填寫指引 |
| :--- | :--- | :--- |
| `game_enabled` | `true` 或 `false` | 控制普通玩家是否能登入。填入 `false` 時，玩家登入會顯示維護中。 |
| `force_logout_time`| 時間戳記 (毫秒數，如 `1718000000000`) | 大掌櫃總開關啟動時間。玩家最後活躍時間若小於此值，將會被強制登出。 |
| `exp_task_complete` | `30` | 完成一個小藥斗任務所獲得的基礎經驗值。必須為正整數。 |
| `exp_task_duplicate`| `55` | 任務通關獎勵的卡牌若已擁有，自動轉換為經驗值的補償數值。 |
| `exp_battle_win` | `50` | 醫道切磋勝利獲得的經驗值。必須為正整數。 |
| `exp_battle_lose` | `20` | 醫道切磋敗北獲得的經驗值。必須為正整數。 |
| `exp_battle_draw` | `30` | 醫道切磋和局獲得的經驗值。必須為正整數。 |
| `exp_qr_duplicate` | `80` | 掃描 QR 碼領取重複卡牌時，轉換為經驗值的補償數值。 |
| `exp_bingo_line` | `150` | 藥斗任務連線（Bingo）一條時所獲得的經驗值。 |
| `exp_bingo_duplicate`| `50` | 連線隨機贈卡重複時，轉換為經驗值的補償數值。 |

---

## 👥 2. players (弟子玩家資料表)
本表儲存所有註冊的弟子與管理員帳號檔案。

| 欄位名稱 | 範例與資料格式 | 說明與填寫指引 |
| :--- | :--- | :--- |
| **id** | `player1`, `admin` | 帳號登入帳號（ID），英文字母與數字。不重複。 |
| **password** | `123`, `admin123` | 登入密碼。明文字串。 |
| **name** | `試藥弟子小智` | 玩家的暱稱，顯示於大廳及切磋對局中。 |
| **role** | `player`, `game_admin`, `admin` | 角色權限。`player` 代表一般弟子；`game_admin` 代表分藥小掌櫃；`admin` 代表大掌櫃。 |
| **level** | `1` 到 `99` | 弟子的醫術等級。大掌櫃可隨意修改。 |
| **exp** | `120` | 弟子的累積經驗值。 |
| **equipped_head** | `equip_head_01` | 已裝備的頭部卡牌 ID，需對應 `cards` 表中的 ID。留空代表無裝備。 |
| **equipped_body** | `equip_body_02` | 已裝備的身軀卡牌 ID。 |
| **equipped_hands**| `equip_hands_02` | 已裝備的手部卡牌 ID。 |
| **equipped_feet** | `equip_feet_02` | 已裝備的腳部卡牌 ID。 |
| **equipped_sub1** | `equip_sub_01` | 已裝備的隨身香包卡牌 ID。 |
| **equipped_sub2** | `equip_sub_02` | 已裝備的避邪葫蘆卡牌 ID。 |
| **deck** | `["skill_card_01", ...]` | 方劑牌組配置。為包含 10 個卡牌 ID 的 JSON 陣列字串。 |
| **inventory** | `{"skill_card_01": 2, ...}` | 背包中擁有的卡牌與數量。為 JSON 物件字串，格式為 `{"卡牌ID": 擁有數量}`。 |
| **tasks_progress**| JSON 物件字串 | 記錄 16 宮格小任務的通關與密碼狀態。 |
| **last_active** | `1718000000000` | 玩家最後活躍時間戳記。用於心跳檢測與強制踢人判定。 |

---

## ⚔️ 3. battles (戰鬥房間表)
儲存所有進行中或歷史對戰房間狀態（通常由系統自動讀寫，不建議手動編輯）。

* **battle_id**：對局的唯一識別碼 (如 `BAT_1718000000`)。
* **p1_id** / **p2_id**：參戰的雙方玩家 ID。
* **status**：對戰狀態（`active` 進行中 / `ended` 結束 / `abandoned` 放棄）。
* **winner_id**：勝利者 ID（如 `player1`、`DRAW` 代表和局）。
* **round_number**：當前對戰回合數 (1 至 5)。
* **r1_p1_cards** 到 **r5_p2_cards**：各回合雙方出牌的記錄。

---

## ✉️ 4. invitations (對戰邀請表)
玩家之間發送的即時切磋邀請（系統自動讀寫）。

* **invitation_id**：邀請唯一識別碼。
* **sender_id**：發送邀請的玩家 ID。
* **receiver_id**：接收邀請的玩家 ID。
* **status**：邀請狀態（`pending` 等待中 / `accepted` 接受 / `rejected` 拒絕 / `expired` 超時）。

---

## 💊 5. admin_quotas (管理員發藥額度表)
用來限制工作人員（`game_admin`）每天或每場活動中，可以發放特定藥貼卡牌的數量。

| 欄位名稱 | 範例 | 說明與填寫指引 |
| :--- | :--- | :--- |
| **admin_id** | `staff` | 工作人員（一般管理員）的帳號 ID。 |
| **card_id** | `skill_card_01` | 要限制配額的卡牌 ID。 |
| **quota** | `10` | 該人員目前所剩下的可發放次數。每次點擊生成 QR Code，此數值會自動減 1。 |

---

## 🔍 6. qr_codes (QR Code 領取憑證表)
記錄已經產生之兌換 QR Code 的 Token，用於驗證是否重複領取（系統自動讀寫）。

* **token**：QR Code 網址內所帶的唯一代碼 (如 `QR_17180000_123`)。
* **card_id**：該 QR Code 兌換的卡牌 ID。
* **status**：`active` 可兌換 / `claimed` 已被領取。
* **claimed_by**：兌換該 QR 碼的玩家 ID。

---

## 🎴 7. cards (卡牌主資料庫表)
這是整場遊戲的藥物資料庫，大掌櫃可以在此增減卡牌、調整裝備屬性或方劑效果。

| 欄位名稱 | 資料類型 | 說明與填寫指引 |
| :--- | :--- | :--- |
| **id** | 字串 (不重複) | 卡牌唯一 ID。如裝備為 `equip_head_01`，技能為 `skill_card_01`。 |
| **name** | 字串 | 卡牌名稱，如 `太陽穴金針`、`人參生脈飲`。 |
| **type** | `equipment` 或 `skill` | 卡牌類型。`equipment` 為靈獸裝備加護，`skill` 為出戰方劑技能。 |
| **sub_type** | 字串 | 子類型。裝備為 `head`/`body`/`hands`/`feet`/`sub`；技能固定填 `skill`。 |
| **element** | `金`, `木`, `水`, `火`, `土` | 卡牌的五行屬性，用於判定相生相剋。 |
| **rarity** | `綠色`, `藍色`, `紫色`, `金色`, `紅色` | 卡牌稀有度，會影響前端卡框顏色。 |
| **atk_mod** | 數值 | 裝備增加的初始藥力 (攻擊力)。 |
| **def_mod** | 數值 | 裝備增加的初始禦力 (防禦力)。 |
| **description** | 字串 | 卡牌的效果文字描述，會顯示在預覽與詳情中。 |
| **image_url** | 網址字串 | 卡牌的精美圖片網址 (可使用 Unsplash 圖片網址)。 |
| **self_atk** / **self_def**...| 條件字串 | 技能方劑的觸發條件限制。留空代表無條件觸發。 |
| **atk_aft** / **def_aft**... | 數值 | 技能觸發後的數值變動，例如 `hp_aft` 設為 `30` 代表自身恢復 30 氣血。 |

---

## 🎒 8. tasks_config (小任務設定工作表)
此表設定 16 宮格藥斗櫃任務的題目與獎勵。

| 欄位名稱 | 範例 | 說明與填寫指引 |
| :--- | :--- | :--- |
| **grid_index** | `0` 到 `15` | 宮格索引。對應 16 宮格藥斗的格子位置 (由左至右、由上至下)。 |
| **name** | `當歸` | 任務名稱，代表該藥斗的主治中藥。 |
| **description** | `辨識當歸切片，完成當歸補血湯配藥。` | 大廳顯示的簡短任務目標。 |
| **reward_card_id**| `skill_card_01` | 完成該任務會贈送的卡牌 ID。若留空則代表該格子純給予通關經驗值。 |
| **detail** | `中藥常識：當歸味甘辛，性溫。歸肝、心、脾經。` | 任務的詳細規則與中藥知識。會於分藥面板點擊 `ⓘ` 時完整彈出展示。 |

---

## 📝 9. active_tasks (進行中任務工作表)
此表儲存所有弟子當前正在進行的小任務，且包含只有大掌櫃與分藥小掌櫃才能看到的通關認證碼（口令）。

| 欄位名稱 | 範例與資料格式 | 說明與填寫指引 |
| :--- | :--- | :--- |
| **player_id** | `player1` | 正在進行該任務的弟子玩家帳號 ID。 |
| **grid_index** | `0` 到 `15` | 所挑戰的藥斗櫃宮格索引。 |
| **password** | `TASK-0-2849` | 該格子挑戰的通關密碼（認證口令）。只有管理員面板和此試算表可見，普通弟子必須向執藥工作人員口述或由工作人員輸入此碼才能通關。 |
| **status** | `active` 或 `completed` | 任務狀態。`active` 代表進行中；`completed` 代表已完成。 |
| **created_at** | 時間戳記 (毫秒數) | 任務發起的時間點。 |
